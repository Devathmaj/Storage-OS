package services

// TrashFile marks a file as trashed or moves it to a trash location.
// This stub should update metadata and optionally move the underlying object.
func TrashFile(fileID string, userID string) error {
    _ = fileID
    _ = userID
    return nil
}
